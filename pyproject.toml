[project]
name = "attention-lr-comp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dataclasses>=0.8",
    "pandas>=3.0.3",
    "pytest>=9.0.3",
    "torch>=2.12.0",
    "tyro>=1.0.13",
    "wandb>=0.27.0",
]
